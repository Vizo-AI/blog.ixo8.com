{
  "name": "vizo-ai-research",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist && npm run check:routes && npm run check:links",
    "check:links": "node scripts/check-internal-links.mjs",
    "check:routes": "node scripts/check-route-parity.mjs",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^7.2.0",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
